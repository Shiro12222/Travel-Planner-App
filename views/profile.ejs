<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
            background: #f8f9fa;
        }
        .background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://images.pexels.com/photos/1831271/pexels-photo-1831271.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1') no-repeat center center fixed;
            background-size: cover;
            z-index: -1; 
            filter: blur(8px);
        }
        .content-wrapper {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 50px 20px; 
        }
        .profile-container {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            max-width: 600px;
            width: 90%;
            text-align: center;
        }
        h1 {
            font-size: 1.8rem;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            margin-bottom: 20px;
            border-collapse: collapse;
        }
        table td {
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }
        .btn-container {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
        }
        .btn-container .btn {
            flex: 1;
            margin: 0 5px;
        }
        .danger-zone {
            text-align: center;
            padding: 15px;
            background: #ffe6e6;
            border-radius: 8px;
            margin-top: 20px;
        }
        .danger-zone p {
            color: #e74c3c;
            margin-bottom: 10px;
            font-weight: bold;
        }
    </style>
</head>
<body>
    
    <div class="background"></div>

    <div class="content-wrapper">
        <div class="profile-container">
            <h1><b>User Details</b></h1>
            <table>
                <tr>
                    <td><strong>Username:</strong></td>
                    <td><%= user.name %></td>
                </tr>
                <tr>
                    <td><strong>Email:</strong></td>
                    <td><%= user.email %></td>
                </tr>
            </table>
            <div class="btn-container">
                <a href="/dashboard" class="btn btn-primary">Go to Dashboard</a>
                <a href="/logout" class="btn btn-secondary">Logout</a>
            </div>
            <div class="btn-container">
                <a href="/updateProfile" class="btn btn-warning w-100">Update Profile</a>
            </div>
            <div class="danger-zone">
                <p>Deleting your account will permanently remove all your data and cannot be undone.</p>
                <form action="/account/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete your account? This action cannot be undone.');">
                    <button type="submit" class="btn btn-danger w-100">Delete My Account</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
